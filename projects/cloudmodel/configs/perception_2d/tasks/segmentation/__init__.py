# Each task script should contain at least these modules:
# dataloaders (train, val and test if needed), networks and metrics.

# The script that builds the final multitask graph model relies on
# these modules to construct each task's subnet.
